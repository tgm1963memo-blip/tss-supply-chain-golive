# Frontend (Vite) — required for the golive app
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Express read-only sync (Python scripts only — never expose service role to Vite)
EXPRESS_DBF_PATH=
ERP_BASE_PATH=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SYNC_ROOM_CODE=TSS
